<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solicitação de Orçamento</title>
    <link rel="stylesheet" href="css/orcamento.css">
</head>
<body>

    <header>
        <div class="logo">
            <img src="imagens/LOGO_SOLLARIUM (8).png" alt="Logo">
        </div>
        <nav class="navbar">
            <ul class="nav-links">
                <li><a href="#home">Início</a></li>
                <li><a href="#about">Sobre</a></li>
                <li><a href="#contact">Contato</a></li>
            </ul>
            <div class="menu-toggle" id="menu-toggle">
                <div class="bar"></div>
                <div class="bar"></div>
                <div class="bar"></div>
            </div>
        </nav>
    </header>

    <main>
        <section id="contact">
            <form id="orcamento-form">
                <h1>Solicitação de Orçamento</h1>
                <div class="form-group">
                    <label for="name">Nome:</label>
                    <input type="text" id="name" name="name" required>
                </div>
                
                <div class="form-group">
                    <label for="text">Serviço:</label>
                    <textarea id="text" name="serviço" rows="4" required></textarea>
                </div>

                <div class="form-group">
                    <label for="valor">Valor esperado:</label>
                    <input type="text" id="valor" name="valor" required>
                </div>

                <div class="form-group">
                    <label for="data">Dia e horário que o serviço deve ser realizado:</label>
                    <input type="datetime-local" id="data" name="data" required>
                </div>
                
                <button type="submit">Enviar Solicitação</button>
                <div id="success-message" style="display: none; color: green; margin-top: 1rem;">
                    Sua solicitação foi enviada com sucesso! Logo um profissional será encontrado para te atender.
                </div>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 Sollarium. Todos os direitos reservados.</p>
    </footer>

    <script>
        document.getElementById('menu-toggle').addEventListener('click', function() {
            const navLinks = document.querySelector('.nav-links');
            navLinks.classList.toggle('active');
        });

        document.getElementById('orcamento-form').addEventListener('submit', function(event) {
            event.preventDefault(); // Previne o envio do formulário para a ação padrão

            // Valida se todos os campos são preenchidos
            const name = document.getElementById('name').value;
            const text = document.getElementById('text').value;
            const valor = document.getElementById('valor').value;
            const data = document.getElementById('data').value;

            if (name && text && valor && data) {
                // Mostra a mensagem de sucesso
                document.getElementById('success-message').style.display = 'block';
            } else {
                // Opcional: Mostrar uma mensagem de erro se não todos os campos estiverem preenchidos
                alert('Por favor, preencha todos os campos.');
            }
        });

        // Tutorial Script
        document.addEventListener('DOMContentLoaded', function() {
            const tutorialOverlay = document.getElementById('tutorial-overlay');
            const steps = document.querySelectorAll('.tutorial-step');
            let currentStep = 1;

            function showStep(step) {
                steps.forEach((el, index) => {
                    el.style.display = index === step - 1 ? 'block' : 'none';
                });
                document.querySelectorAll('.tutorial-tip').forEach((tip, index) => {
                    tip.style.display = index === step - 1 ? 'block' : 'none';
                });
            }

            showStep(currentStep);

            document.getElementById('next-step').addEventListener('click', function() {
                if (currentStep < steps.length) {
                    currentStep++;
                    showStep(currentStep);
                }
            });

            document.getElementById('next-step-2').addEventListener('click', function() {
                if (currentStep < steps.length) {
                    currentStep++;
                    showStep(currentStep);
                }
            });

            document.getElementById('next-step-3').addEventListener('click', function() {
                if (currentStep < steps.length) {
                    currentStep++;
                    showStep(currentStep);
                }
            });

            document.getElementById('skip-tutorial').addEventListener('click', function() {
                tutorialOverlay.style.display = 'none';
            });

            document.getElementById('skip-tutorial-2').addEventListener('click', function() {
                tutorialOverlay.style.display = 'none';
            });

            document.getElementById('skip-tutorial-3').addEventListener('click', function() {
                tutorialOverlay.style.display = 'none';
            });

            document.getElementById('skip-tutorial-final').addEventListener('click', function() {
                tutorialOverlay.style.display = 'none';
            });
            document.addEventListener('DOMContentLoaded', function() {
    const tutorialOverlay = document.getElementById('tutorial-overlay');
    const steps = document.querySelectorAll('.tutorial-step');
    let currentStep = 1;

    function showStep(step) {
        steps.forEach((el, index) => {
            el.style.display = index === step - 1 ? 'block' : 'none';
        });
        document.querySelectorAll('.tutorial-tip').forEach((tip, index) => {
            tip.style.display = index === step - 1 ? 'block' : 'none';
        });
    }

    showStep(currentStep);

    document.getElementById('next-step').addEventListener('click', function() {
        if (currentStep < steps.length) {
            currentStep++;
            showStep(currentStep);
        }
    });

    document.getElementById('next-step-2').addEventListener('click', function() {
        if (currentStep < steps.length) {
            currentStep++;
            showStep(currentStep);
        }
    });

    document.getElementById('next-step-3').addEventListener('click', function() {
        if (currentStep < steps.length) {
            currentStep++;
            showStep(currentStep);
        }
    });

    document.getElementById('skip-tutorial').addEventListener('click', function() {
        tutorialOverlay.style.display = 'none';
    });

    document.getElementById('skip-tutorial-2').addEventListener('click', function() {
        tutorialOverlay.style.display = 'none';
    });

    document.getElementById('skip-tutorial-3').addEventListener('click', function() {
        tutorialOverlay.style.display = 'none';
    });

    document.getElementById('skip-tutorial-final').addEventListener('click', function() {
        tutorialOverlay.style.display = 'none';
    });
});

        });
    </script>
</body>
</html>
